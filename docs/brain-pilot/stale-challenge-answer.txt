**The reviewer is incorrect for the supplied source snapshot.** Apps-only preview rejects `/__preview/manifest` with **403**, not 200.

- **Decisive source:** `servesGames()` is true only for `all` or `games` ([scripts/preview-service.mjs:13–21](scripts/preview-service.mjs#L13-L21)). The manifest handler explicitly returns `403 {"error":"forbidden"}` when that predicate is false, before reading the game manifest ([lines 125–128](scripts/preview-service.mjs#L125-L128)).
- **Existing tests corroborate this:** the apps-mode test expects wrapper **403** and static `/apps/manifest.json` **200** ([tests/preview-workflow.spec.mjs:127–144](tests/preview-workflow.spec.mjs#L127-L144)); games mode expects wrapper **200** ([lines 147–154](tests/preview-workflow.spec.mjs#L147-L154)). These are inspected assertions, **not observed test passes**.
- **The card is stale and internally contradictory:** its apps row and invariant 4 claim success ([docs/brain/preview-routing.md:14](docs/brain/preview-routing.md#L14), [line 26](docs/brain/preview-routing.md#L26)), while its own test summary says apps-only **403** ([line 42](docs/brain/preview-routing.md#L42)). Its all-zero revision and old inspection date do not establish applicability to the supplied baseline ([line 3](docs/brain/preview-routing.md#L3)). **Actual source, corroborated by test assertions, wins over this derivative documentation.**

**Corrections recommended—not applied:** change the apps table entry to 403; describe the wrapper as game-gated (`all`/`games`), retaining `/apps/manifest.json` as the app manifest. Refresh provenance to the supplied baseline `a3da91f69d24c14f6612b2a1fcdae7d2abb4f1b4`, explicitly labeled static inspection with tests not run. No source/test change is justified merely to match the stale card.

**Still unknown:** actual test results, running/deployed revision and environment, service availability, proxy/DNS/TLS behavior, externally observed endpoint responses, and downstream integrations. Source health reports `ok: true` without establishing these facts ([scripts/preview-service.mjs:46–60](scripts/preview-service.mjs#L46-L60)). The baseline identifier was supplied, not independently verified; `.git` is absent by design.

**Scope/count:** started with the card, then read source and tests: **3 read/search/terminal calls** (all reads), plus **1 required skill-load call**. No files created or modified; no tests, services, network requests, or writes performed.